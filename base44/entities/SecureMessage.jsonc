{
  "name": "SecureMessage",
  "type": "object",
  "properties": {
    "organization_id": {
      "type": "string",
      "description": "Tenant organization"
    },
    "location_id": {
      "type": "string",
      "description": "Message location"
    },
    "patient_id": {
      "type": "string",
      "description": "Related patient"
    },
    "from_user_id": {
      "type": "string",
      "description": "Sender user ID"
    },
    "from_user_email": {
      "type": "string",
      "description": "Sender email"
    },
    "to_user_id": {
      "type": "string",
      "description": "Recipient user ID"
    },
    "to_user_email": {
      "type": "string",
      "description": "Recipient email"
    },
    "subject": {
      "type": "string",
      "description": "Message subject"
    },
    "message_body": {
      "type": "string",
      "description": "Message content"
    },
    "sent_at": {
      "type": "string",
      "format": "date-time",
      "description": "Send timestamp"
    },
    "read_at": {
      "type": "string",
      "format": "date-time",
      "description": "Read timestamp"
    },
    "priority": {
      "type": "string",
      "enum": [
        "normal",
        "high",
        "urgent"
      ],
      "default": "normal",
      "description": "Message priority"
    },
    "message_type": {
      "type": "string",
      "enum": [
        "clinical",
        "administrative",
        "billing",
        "general"
      ],
      "default": "general",
      "description": "Message category"
    },
    "attachments": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Attachment URLs"
    },
    "is_encrypted": {
      "type": "boolean",
      "default": true,
      "description": "HIPAA encryption status"
    }
  },
  "required": [
    "from_user_id",
    "to_user_id",
    "message_body",
    "sent_at"
  ],
  "rls": {
    "create": {
      "$or": [
        {
          "data.from_user_email": "{{user.email}}"
        },
        {
          "data.from_user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "read": {
      "$or": [
        {
          "data.from_user_email": "{{user.email}}"
        },
        {
          "data.to_user_email": "{{user.email}}"
        },
        {
          "data.from_user_id": "{{user.id}}"
        },
        {
          "data.to_user_id": "{{user.id}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "data.from_user_email": "{{user.email}}"
        },
        {
          "data.to_user_email": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      }
    }
  }
}